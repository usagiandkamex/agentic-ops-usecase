---
mode: agent
description: 'Azure リソースを信頼性・可用性の観点で読み取り専用分析する。'
---

# 信頼性分析（Reliability Analysis）

対象の Azure サブスクリプション / リソースグループを **信頼性・可用性** の観点で分析してください。
未指定の場合は分析対象（`<SUBSCRIPTION_ID>` / `<RESOURCE_GROUP>`）を最初に確認します。

## 収集する情報（読み取り専用）

- Azure MCP ツールを優先し、利用不可なら Azure CLI (`az`) を代替提示する。
- 主なチェック対象:
  - 冗長構成（可用性ゾーン / 可用性セット / リージョン冗長）の有無
  - バックアップ / スナップショット / 復旧ポイントの構成
  - ストレージ / DB の冗長オプション（LRS/ZRS/GRS 等）
  - 単一障害点（SPOF）、負荷分散・フェールオーバー構成
  - SLA に影響する構成上のギャップ

## 出力

次の列を持つ表で、影響の大きい順に指摘を示してください。

| 優先度 | リソース種別 | 指摘 | 推奨アクション |
| --- | --- | --- | --- |

最後に、可用性リスクの要約と、優先的に対処すべき項目をまとめてください。

## 注意

- 破壊的操作や構成変更は行わず、推奨アクションの提示に留める。
- 実 ID・リソース名を含めず、プレースホルダを使用する。
